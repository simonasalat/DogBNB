
namespace DogBNB.Models
{
    public enum ServiceType
    {
        Boarding, //Dog stays over at host's home
        Walking,
        Sitting, //Sitter stays over at dog owner's home
        DayCare, //Dog spends day at host's home
        DropIn //Sitter spends a day at owner's home
    }
}
